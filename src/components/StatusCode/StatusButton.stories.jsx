import { h } from 'preact';
import StatusButton from '../StatusCode/StatusButton';

export default {
    title: 'Component/StatusButton',
    component: StatusButton,
};

export const Default = () => <StatusButton />;
